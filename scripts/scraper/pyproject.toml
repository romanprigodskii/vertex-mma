[project]
name = "vertex-mma-scraper"
version = "0.1.0"
description = "UFC Stats scraper for Vertex MMA"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]
